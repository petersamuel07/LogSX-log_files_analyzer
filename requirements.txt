# Runtime dependencies are declared in pyproject.toml ([project.dependencies]).
# This file installs the project itself in editable mode, which pulls them in —
# so there is only one dependency list to keep up to date, not two.
#
#   pip install -r requirements.txt
-e .
