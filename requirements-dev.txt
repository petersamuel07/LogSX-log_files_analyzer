# Runtime dependencies plus the tooling needed to test, lint, and package the
# app (pytest, ruff, PyInstaller) — declared in pyproject.toml under
# [project.optional-dependencies] dev. Not required to run the app from source.
#
#   pip install -r requirements-dev.txt
-e .[dev]
